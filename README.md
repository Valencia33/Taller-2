# Taller-2

De una la estructura de diferentes tipos de empleados da a entender que se habla de una clase abstracta Empleado de la cual heredan las otras dos y también deja abierta la puerta a añadir más empleados con diferentes comportamientos. Entonces lo primero que hice fue escribir la estructura de estas tres clases, esto incluye el constructor y las variables que sé QUE DEBEM TENER y solo las declaré con sus métodos accesores y ya, ambos públicos pq pues son metodos accesores y se supone que la validación de los datos de estos se hace ahí dentro, pero como el ejercicio no pide ese tipo de validación no hice nada más, esto en conjunto con los métodos que necesita cada una, que estas las heredan de la clase Empleado y ya cada una la reescribe a su forma. Lo siguiente fue buscar como es que era que se escribía bien el constructor cuando hereda pq no me acordaba, le añadí el base y ahí mismo le asigné el valor q se passa como parametro a los metodos accesores. Eso fue en el caso de Empleado pq aún no sabía que atributos iba a utilizar en los otros dos, igual seguí el mismo proceso para los otros dos tipos de empleado, el de horas tiene dos atributos las horas  y q vale cada hora, ACÁ si hay que ser claro y decir q DEBEN ser privadas pq son VALORES FIJOS, esto tiene un nombre, no recuerdo, era como atributos algo, pero el chiste de estos es q SOLO se pueden modificar desde la misma clase, y por eso son privados, pq es un valor que no debería ser cambiado por demás clases (sobretodo cuando el objetivo del programa no es definir salarios sino solo manejar los empleados y visualizar diferentes aspectos de ellos, más no editarlos).

Pido perdón por escribir todo eso en un solo parrafo, voy a morir. Ya lo último fue sobreescribir los métodos en cada una de las subclases, que eso ya si es lógica muy simple entonces no tardó mucho, en el de salario fijo fue devolver la misma variable con el salario y en el otro fue multiplicar el valor por las horas. El otro método es un Console.WriteLine q llama el método anteriormente descrito pq tiene q mostrar q tipo es, el nombre y su salario y yap.

NO ME ACORDABA QUE ERA UNA INTERFAZ y la verdad sigo sin estar muy seguro de q es pero si mal no recuerdo es simplemente una calse que define un MONTÓN de métodos (exageré, puede ser uno) y así las clases que heredan de ella los pueden usar, el tema es q el comportamiento de cada método depende de la clase. La vdd no recuerdo que uso era al que se le daba a estos cositos, pero como todos los empleados tienen los mismo métodos entonces no más fue escribir la firma de mostrarInfo ahí en la interfaz y poner que EMpleado heredadba de la interfaz y ya cada método la sobreescribe y pone el comportamiento que le de la gana.

perdón por cualquier error ortográfico tengo mucho afán ahhhh
